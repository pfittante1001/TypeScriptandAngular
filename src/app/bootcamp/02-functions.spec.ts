describe('functions', () => {
  describe('function literals', () => {
    it('has a few kinds', () => {
      function add(a: number, b: number): number {
        return a + b;
      }
    });
  });
});
